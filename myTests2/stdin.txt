.IPPcode19
# Ukazka kódu se sémantickou chybou (skript parse.php ale vrací 0 místo 54), která nemá být detekována už v parse.php. Obsahuje příklad elementu bez podelementů, který lze zapsat krátkým i dlouhým způsobem.
DEFVAR TF@x
CREATEFRAME # příklad instrukce bez operandů, v XML možný zápis zkráceným způsobem
WRITE  TF@x
POPFRAME # tato instrukce bude pro ilustraci ve výstupním XML zapsána druhým možným způsobem

READ TF@x string
Write string@ahoj\077ahoj
